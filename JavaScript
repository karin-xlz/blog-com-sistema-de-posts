document.addEventListener('DOMContentLoaded', function() {
    // Dados das dicas de autocuidado
    const autocuidadoTips = [
        {
            icon: "💆‍♀️",
            title: "Respiração Consciente",
            content: "Pare por 1 minuto e respire profundamente 3 vezes. Inspire por 4 segundos, segure por 4 e expire por 6. Isso acalma o sistema nervoso."
        },
        {
            icon: "💧",
            title: "Hidratação",
            content: "Mantenha uma garrafa de água sempre perto. Beber água regularmente melhora a disposição, a pele e a digestão."
        },
        {
            icon: "🛌",
            title: "Rotina do Sono",
            content: "Estabeleça um horário fixo para dormir e acordar. Desligue as telas 1 hora antes de dormir para melhorar a qualidade do sono."
        },
        {
            icon: "🙅‍♀️",
            title: "Aprenda a Dizer Não",
            content: "Proteger seu tempo e energia não é egoísmo. Pratique dizer não sem culpa para compromissos que não te servem."
        },
        {
            icon: "📝",
            title: "Journaling Matinal",
            content: "Escreva 3 coisas pelas quais você é grata e 3 prioridades do dia. Isso traz clareza e foco para sua rotina."
        }
    ];

    // Elementos do slider
    const sliderTrack = document.querySelector('.slider-track');
    const dotsContainer = document.querySelector('.slider-dots');
    const prevBtn = document.querySelector('.prev-btn');
    const nextBtn = document.querySelector('.next-btn');

    let currentSlide = 0;

    // Criar slides
    function createSlides() {
        autocuidadoTips.forEach((tip, index) => {
            // Criar slide
            const slide = document.createElement('div');
            slide.className = 'slide';
            slide.innerHTML = `
                <div class="slide-icon">${tip.icon}</div>
                <h3>${tip.title}</h3>
                <p>${tip.content}</p>
            `;
            sliderTrack.appendChild(slide);

            // Criar pontos de navegação
            const dot = document.createElement('div');
            dot.className = 'dot';
            dot.dataset.index = index;
            dotsContainer.appendChild(dot);
            
            dot.addEventListener('click', () => {
                goToSlide(index);
            });
        });

        updateDots();
    }

    // Atualizar pontos de navegação
    function updateDots() {
        const dots = document.querySelectorAll('.dot');
        dots.forEach((dot, index) => {
            dot.classList.toggle('active', index === currentSlide);
        });
    }

    // Navegar para slide específico
    function goToSlide(index) {
        currentSlide = index;
        sliderTrack.style.transform = `translateX(-${currentSlide * 100}%)`;
        updateDots();
    }

    // Slide anterior
    function prevSlide() {
        if (currentSlide > 0) {
            currentSlide--;
        } else {
            currentSlide = autocuidadoTips.length - 1;
        }
        goToSlide(currentSlide);
    }

    // Próximo slide
    function nextSlide() {
        if (currentSlide < autocuidadoTips.length - 1) {
            currentSlide++;
        } else {
            currentSlide = 0;
        }
        goToSlide(currentSlide);
    }

    // Event listeners
    prevBtn.addEventListener('click', prevSlide);
    nextBtn.addEventListener('click', nextSlide);

    // Iniciar slider
    createSlides();

    // Autoplay (opcional)
    let autoplay = setInterval(nextSlide, 5000);

    // Pausar autoplay quando interagir
    sliderTrack.addEventListener('mouseenter', () => {
        clearInterval(autoplay);
    });

    sliderTrack.addEventListener('mouseleave', () => {
        autoplay = setInterval(nextSlide, 5000);
    });

    // Sistema de posts existente (mantido do código original)
    const postsData = [
        {
            id: 1,
            date: '22 de Maio, 2023',
            title: 'Mindfulness para Iniciantes',
            excerpt: 'Aprenda técnicas simples de mindfulness para reduzir o estresse e aumentar o foco no seu dia a dia.',
            image: 'https://source.unsplash.com/random/600x400/?meditation'
        },
        {
            id: 2,
            date: '29 de Maio, 2023',
            title: 'Alimentação Consciente',
            excerpt: 'Como desenvolver uma relação saudável com a comida e ouvir os sinais do seu corpo.',
            image: 'https://source.unsplash.com/random/600x400/?healthy,food'
        }
    ];
    
    const postsContainer = document.querySelector('.posts');
    
    function addPosts() {
        postsData.forEach(post => {
            const postElement = document.createElement('article');
            postElement.className = 'post';
            postElement.innerHTML = `
                <div class="post-image">
                    <img src="${post.image}" alt="${post.title}">
                </div>
                <div class="post-content">
                    <p class="post-date">${post.date}</p>
                    <h2 class="post-title">${post.title}</h2>
                    <p class="post-excerpt">${post.excerpt}</p>
                    <a href="#" class="read-more" data-id="${post.id}">Continuar Lendo</a>
                </div>
            `;
            postsContainer.appendChild(postElement);
        });
    }
    
    addPosts();
    
    const newsletterForm = document.querySelector('.newsletter-form');
    if (newsletterForm) {
        newsletterForm.addEventListener('submit', function(e) {
            e.preventDefault();
            alert('Obrigada por assinar nossa newsletter! Em breve você receberá nossas novidades.');
            newsletterForm.reset();
        });
    }
});
